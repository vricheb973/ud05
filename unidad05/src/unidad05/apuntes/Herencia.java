package unidad05.apuntes;

public class Herencia {
	
	/*
	 * Dibujar el diagrama de clases
	 * Decidir qué clases son abstractas y cuáles no
	 * Creo las clases respetando la herencia (con constructor y getters/setters)
	 * Genero toString e equals supertipo
	 * 	toString: Usamos el String.format obligatorio
	 * 	equals: ponemos ifs del padre y modificamos la última línea
	 * 
	 * Pongo atención a los enumerados (hay que tratarlos como Strings).
	 */

}
